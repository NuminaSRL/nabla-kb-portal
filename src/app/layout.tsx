import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "NABLA KB Portal - Italian/EU Regulatory Compliance Knowledge Base",
  description: "Direct access to comprehensive Italian and EU regulatory compliance documents including GDPR, AI Act, D.Lgs 231, Tax, AML, and Contract Law",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
